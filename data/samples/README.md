# Synthetic sample data

These files match the competition schema without including challenge records or product images.
All names, values, links, and prices are fictional. Use them for command examples and validation.

Download authorised train, test, sample-test, and sample-output files into the Git-ignored
`data/raw/` directory with `scripts/download_kaggle_assets.py`.
